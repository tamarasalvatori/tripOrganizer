# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'TripOrganizer' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for TripOrganizer
pod 'Firebase/Core'
pod 'Firebase/Auth'
pod 'GoogleSignIn'

  target 'TripOrganizerTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'TripOrganizerUITests' do
    # Pods for testing
  end
end
